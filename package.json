{
  "name": "cc-cookiecomply",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && npm run prepare-pages",
    "build:pages": "next build && npm run prepare-pages",
    "prepare-pages": "node -e \"const fs=require('fs');const path=require('path');if(fs.existsSync('dist'))fs.rmSync('dist',{recursive:true});fs.mkdirSync('dist');function copyDir(src, dest) {if(!fs.existsSync(dest))fs.mkdirSync(dest,{recursive:true});const items=fs.readdirSync(src);for(const item of items) {if(item==='cache')continue;const srcPath=path.join(src,item);const destPath=path.join(dest,item);const stat=fs.statSync(srcPath);if(stat.isDirectory())copyDir(srcPath,destPath);else if(stat.size<25*1024*1024)fs.copyFileSync(srcPath,destPath);}}copyDir('.next','dist');console.log('dist folder created for Cloudflare Pages (excluding large cache files)');\"",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@hookform/resolvers": "^3.3.2",
    "framer-motion": "^10.16.4",
    "lucide-react": "^0.292.0",
    "next": "^15.5.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-hook-form": "^7.48.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "@types/react": "^18.2.37",
    "@types/react-dom": "^18.2.15",
    "autoprefixer": "^10.4.16",
    "eslint": "^8.57.1",
    "eslint-config-next": "^14.0.0",
    "postcss": "^8.4.31",
    "tailwindcss": "^3.3.5",
    "typescript": "^5.2.2"
  }
}
